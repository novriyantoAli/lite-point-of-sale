<script lang="ts">
	import { PengaturanForm } from '$lib/domains/pengaturan';

	// The screen owns nothing but its heading; the route is only the adapter that
	// puts the domain component on a URL (ADR-0006).
</script>

<PengaturanForm />
