<script lang="ts">
	import { StokList } from '$lib/domains/produk';

	// The screen owns its own heading and both of its lists; the route is only the
	// adapter that puts the domain component on a URL.
</script>

<StokList />
