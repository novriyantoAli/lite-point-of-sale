<script lang="ts">
	import { Kasir } from '$lib/domains/penjualan';

	// The screen owns its heading and every part of the sale flow; the route is
	// only the adapter that puts the domain component on a URL.
</script>

<Kasir />
