<script lang="ts">
	import { ProdukList } from '$lib/domains/produk';

	// The screen owns its own heading and its Tambah button, which sit on the same
	// row: the route is only the adapter that puts the domain component on a URL.
</script>

<ProdukList />
