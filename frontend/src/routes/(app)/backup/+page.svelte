<script lang="ts">
	import { BackupPanel } from '$lib/domains/backup';

	// The screen owns nothing: the route is only the adapter that puts the domain
	// component on a URL (ADR-0006). The heading lives in the component.
</script>

<BackupPanel />
